{
  "name": "emovizz",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "node server",
    "dev": "nodemon server",
    "postinstall": "cd .. && cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.21"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "openai": "^3.2.1"
  }
}
